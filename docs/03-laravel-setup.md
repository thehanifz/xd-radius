# 03 — Laravel Setup

> 🚧 **Coming Soon** — Dokumentasi ini akan diisi setelah instalasi Laravel selesai.

## Yang Akan Dicakup

- Clone repository & install dependencies
- Konfigurasi `.env` untuk PostgreSQL
- Menjalankan migrations (schema FreeRADIUS + app custom)
- Setup Laravel Scheduler
- Menjalankan aplikasi tanpa web server (PHP built-in / FrankenPHP)
- Setup queue worker
